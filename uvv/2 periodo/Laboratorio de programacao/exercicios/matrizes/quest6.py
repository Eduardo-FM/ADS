"""
6) Crie um sistema para controlar produtos em estoque, usando lista de dicionários.
 Exemplo: {"nome": "Teclado", "preco": 150.0, "quantidade": 8}

Funções:

cadastrar_produto(lista)

atualizar_estoque(lista, nome, quantidade)

calcular_valor_total(lista) ? soma o valor de todos os produtos (preço × quantidade).


7)  Crie funções para converter entre Celsius, Fahrenheit e Kelvin.

Funções:

c_para_f(c) ? retorna Fahrenheit

f_para_c(f) ? retorna Celsius

c_para_k(c) ? retorna Kelvin
 Peça ao usuário uma temperatura e a unidade, e exiba as conversões.


8) Dada uma lista de palavras, crie funções que:

maiores_palavras(lista) ? retorna palavras com mais de 6 letras.

contar_com_letra(lista, letra) ? conta quantas palavras contêm determinada letra.

ordenar_por_tamanho(lista) ? retorna a lista ordenada pelo tamanho das palavras.


9) Implemente uma calculadora simples com as funções:

somar(a, b)

subtrair(a, b)

multiplicar(a, b)

dividir(a, b) ? tratar divisão por zero.


Use um menu interativo que permita escolher a operação e repetir até o usuário sair.

10) Dada uma lista de votos:
 votos = ["Maria", "João", "Ana", "Maria", "João", "Maria"]

Crie funções:

contar_votos(votos) ? retorna um dicionário com a contagem.

vencedor(votos) ? retorna o nome do vencedor.

percentuais(votos) ? retorna o percentual de votos de cada candidato.

Mostre o resultado formatado com nome, votos e percentual.
"""


